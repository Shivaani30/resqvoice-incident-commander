import { emptyIntelligence, normalizeSpeech, observe, type Intelligence } from './intelligence.js'
export type Status='pending'|'completed'|'cancelled'|'stale-discarded'|'failed'; export type Tool='createIncident'|'updateSeverity'|'notifyTeam'|'getIncidentStatus'|'cancelNotification';
export type Event={id:string;type:'generation_started'|'tool'|'interrupt'|'stale_discarded'|'intelligence';sessionId:string;turnId:string;generationId:number;timestamp:string;status:Status|'completed';latencyMs?:number;tool?:Tool;message:string}
type Incident={id:string;severity:string;title:string;status:string;team:string;startedAt?:string}; type Session={generationId:number;incident:Incident|null;intelligence:Intelligence;events:Event[];turns:any[];controllers:Map<number,AbortController[]>}
const uid=()=>crypto.randomUUID().slice(0,8); const now=()=>new Date().toISOString(); const sessions=new Map<string,Session>();let number=2847;
export const getSession=(id:string)=>{if(!sessions.has(id))sessions.set(id,{generationId:0,incident:null,intelligence:emptyIntelligence(),events:[],turns:[],controllers:new Map});return sessions.get(id)!};const emit=(s:Session,e:Omit<Event,'id'|'timestamp'>)=>{const x={...e,id:uid(),timestamp:now()};s.events.push(x);return x};const team=(v:string)=>v.match(/\b(backend|payments|platform|on-call)\b/i)?.[1]?.toLowerCase();const priority=(v:string)=>v.match(/\bp([123])\b/i)?.[1]&&`P${v.match(/\bp([123])\b/i)![1]}`;
export function interrupt(sessionId:string,reason='interrupt'){const s=getSession(sessionId),old=s.generationId;s.controllers.get(old)?.forEach(c=>c.abort());for(const e of s.events)if(e.generationId===old&&e.status==='pending')e.status='cancelled';s.generationId++;emit(s,{type:'interrupt',sessionId,turnId:'interrupt',generationId:s.generationId,status:'completed',message:reason});return s}
export async function command(sessionId:string,turnId:string,text:string,delay=3000){const s=getSession(sessionId);interrupt(sessionId,'replacement_command');const generationId=s.generationId,controller=new AbortController();s.controllers.set(generationId,[controller]);s.turns.push({id:turnId,role:'commander',text,timestamp:now(),generation:generationId});emit(s,{type:'generation_started',sessionId,turnId,generationId,status:'completed',message:'New generation'});const p=priority(text),t=team(text),l=text.toLowerCase(),normalized=normalizeSpeech(text);let spoken='I did not understand that command.';if(l.includes('create')&&p){const title=text.match(/for (.+?)(?: and notify|$)/i)?.[1]??'an incident';s.incident={id:`INC-${number++}`,severity:p,title,status:'Investigating',team:'on-call',startedAt:now()};emit(s,{type:'tool',tool:'createIncident',sessionId,turnId,generationId,status:'completed',message:`Incident created`});spoken=`${p} incident created for ${title}.`}if(l.includes('change it to')&&p&&s.incident){s.incident.severity=p;emit(s,{type:'tool',tool:'updateSeverity',sessionId,turnId,generationId,status:'completed',message:`Severity ${p}`});spoken=`Incident changed to ${p}.`}if(l.includes('notify')&&t){const start=performance.now(),e=emit(s,{type:'tool',tool:'notifyTeam',sessionId,turnId,generationId,status:'pending',message:`Notifying ${t}`});spoken+=` I’m notifying ${t} now.`;setTimeout(()=>{if(controller.signal.aborted||generationId!==s.generationId){if(e.status==='pending')e.status='stale-discarded';emit(s,{type:'stale_discarded',tool:'notifyTeam',sessionId,turnId,generationId,status:'stale-discarded',latencyMs:performance.now()-start,message:`${t} fenced`});return}if(e.status==='pending'){e.status='completed';e.latencyMs=performance.now()-start;e.message=`Notified ${t}`;if(s.incident)s.incident.team=t}},delay)}const observation=observe(s.intelligence,text,turnId,now(),s.incident?.status ?? 'Investigating');if(!s.incident&&/(?:checkout|check out|payments?)/i.test(normalized)&&/(?:failures?|errors?|failing|cannot|cant|unable)/i.test(normalized))s.incident={id:`INC-${number++}`,severity:p??'Not recorded',title:'Checkout failures',status:'Investigating',team:'Unassigned',startedAt:now()};if(observation&&!/(?:create|change it to|notify)\b/i.test(l))spoken=observation;emit(s,{type:'intelligence',sessionId,turnId,generationId,status:'completed',message:text});s.turns.push({id:uid(),role:'copilot',text:spoken.trim(),timestamp:now(),generation:generationId});return {session:s,spoken,generationId}}
